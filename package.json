{
  "name": "@scola/validator",
  "version": "5.1.0",
  "description": "Validator",
  "repository": "github:scola84/node-validator",
  "author": "M.C. van den Boogaart <scola@kvdb.name>",
  "license": "MIT",
  "main": "./dist/validator.js",
  "jsnext:main": "./index.js",
  "dependencies": {
    "lodash-es": "^4.15.0"
  },
  "devDependencies": {
    "rollup": "^0.36.1",
    "rollup-plugin-buble": "^0.14.0",
    "rollup-plugin-node-resolve": "^2.0.0",
    "uglify-js": "^2.7.3"
  },
  "scripts": {
    "build": "npm run cleanup && npm run rollup",
    "build:min": "npm run build && npm run min",
    "cleanup": "rm -rf dist",
    "min": "uglifyjs dist/validator.js -m -o dist/validator.min.js",
    "prepublish": "npm run build:min",
    "rollup": "rollup -c"
  }
}
